{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @melodypath/web build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/music/(.*)", "destination": "/music/index.html" }
  ]
}
